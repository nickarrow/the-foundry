name: Enforce Foundry Ownership

on:
  push:
    branches:
      - main

concurrency:
  group: foundry-enforcement
  cancel-in-progress: false

jobs:
  enforce-ownership:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history for rename detection and ownership tracking
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install pyyaml
      
      - name: Configure Git
        run: |
          git config user.name "Foundry Enforcer"
          git config user.email "actions@github.com"
      
      - name: Run enforcement script
        env:
          COMMIT_AUTHOR: ${{ github.event.head_commit.author.username }}
          COMMIT_SHA: ${{ github.sha }}
        run: python .github/scripts/enforce_ownership.py
